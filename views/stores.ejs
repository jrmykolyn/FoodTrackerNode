<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MySQL Data</title>
</head>
<body>
    <h1>Data from MySQL Database</h1>
    <div>
        <% rows.forEach(row => { %>
            <div><%= row.storeName %></div>
            <div><%= row.storeAddress %></div>
            <div><%= row.storePostalCode %></div>
        <% }); %>
    </div>
</body>
</html>